rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open academic archive: allow reading, uploading and downloading without sign-in barriers
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


