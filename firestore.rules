rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /notes/{noteId} {
      // Academic archive: Anyone can read, list, and search school notes
      allow read: if true;
      
      // Allow uploading, editing, and deleting notes
      allow create, update, delete: if true;
    }
  }
}

